{
  "name": "webpack-examples",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "server": "http-server server -p 3000",
    "client": "pack -r --proxy 3000 -a assets -s client -w",
    "build": "rimraf server/assets && pack -r --proxy 3000 -a assets -s client -o server/assets"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "http-server": "^0.9.0",
    "material-ui": "^0.15.4",
    "pack-cli": "^0.2.1",
    "react": "^15.3.2",
    "react-tap-event-plugin": "^1.0.0",
    "rimraf": "^2.5.4"
  }
}
